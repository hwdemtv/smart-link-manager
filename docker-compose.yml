# Docker Compose 配置
# 使用 host 网络模式，连接本地 MySQL

services:
  app:
    build: .
    container_name: smart-link-app
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env
    volumes:
      - ./uploads:/app/uploads
